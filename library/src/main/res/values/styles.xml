<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="colorControlHighlight">@color/colorAccent</item>
    </style>

    <!-- ======================== Activity ======================== -->
    <!-- 常用Act -->
    <style name="AppTheme.Default"/>
    <!-- LauncherStyle -->
    <style name="AppTheme.Launcher">
        <item name="android:windowBackground">@drawable/layer_launcher</item>
        <!--<item name="android:windowDrawsSystemBarBackgrounds">false</item>-->
    </style>
    <!-- LoginAct -->
    <style name="AppTheme.Login"/>
    <!-- MainAct -->
    <style name="AppTheme.Main"/>

    <!-- Search -->
    <style name="AppTheme.Search">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowEnterAnimation">@anim/alpha_in</item>
        <item name="android:windowExitAnimation">@anim/alpha_out</item>
    </style>
    <!-- TransStyle -->
    <style name="AppTheme.Trans">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/ActAnimTrans</item>
    </style>

    <style name="ActAnimTrans" parent="@android:style/Animation"/>
    <!-- ======================== dialog ======================== -->
    <style name="DialogDef" parent="Theme.AppCompat.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="DialogTrans" parent="DialogDef">
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="DialogBtm" parent="DialogDef">
        <item name="android:windowAnimationStyle">@style/DialogAnimBtm</item>
    </style>

    <style name="DialogAnimBtm" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/dialog_btm_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_btm_out</item>
    </style>

    <style name="ProgressBar" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/layer_progress</item>
    </style>
    <!-- ======================== 子模块 ======================== -->
</resources>
